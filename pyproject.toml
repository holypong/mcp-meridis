[project]
name = "mcp-meridis"
version = "0.1.0"
description = "MCP server and Web UI for Meridian humanoid robot control"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "gradio[mcp]>=5.29.0",
]
